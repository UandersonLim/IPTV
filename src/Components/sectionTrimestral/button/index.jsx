import { StyleButton, ContainerButton } from "./style";


const Button = () => {
    return(
        <ContainerButton>
            <StyleButton>ASSINAR</StyleButton>
        </ContainerButton>
    )}

export default Button;